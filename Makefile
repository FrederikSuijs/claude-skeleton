# Stub: include the claude-skeleton make rules from `claude.mk`. To add
# your own targets, write them above or below this line — `include`
# brings in claude.mk as-is. (You can also invoke the rules directly
# with `make -f claude.mk <target>` and skip this stub entirely.)
include claude.mk
